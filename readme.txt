This is a simple SLAM illustration which is written in Matlab.

To use it, please open matlab, cd to this directory and type:

addpath(genpath(pwd))

You can then run the code:

beadSLAM

See beadSLAM.m for documentation and a list of options.
